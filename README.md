Entrar en config.php de la carpeta teampasswordmanager:

```bash
define('CONFIG_USERNAME', 'tpmuser');
define('CONFIG_PASSWORD', 'tpmpassword');
define('CONFIG_DATABASE', 'teampasswordmanager');
```
Luego `docker-compose up --build`